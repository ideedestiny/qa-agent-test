# qa-agent-test

This repo is used as a test target for the QA Agent project.
The QA Agent watches for new pull requests, reads the code changes,
and automatically generates Pytest tests using an LLM.
